use("restaurants");
db.restaurants.count({ borough: { $in: ['Queens','Staten Island','Bronx']  }});

use("restaurants");
db.restaurants.count({ cuisine: { $ne: 'American'  }});

use("restaurants");
db.restaurants.count({ rating: { $gt: 8  }});

use("restaurants");
db.restaurants.count({ rating: { $lt: 4  }});

use("restaurants");
db.restaurants.count({ rating: { $ne: [5, 6, 7] }});
